#include <stdio.h> // Bibliotecas
#include <stdlib.h>

int main (){ //corpo principal do algoritmo
	
	printf("Hello, world!\n"); // comando para imprimir na tela uma mensagem.
	
	system("pause");
	
}

// Anotações:
// 1 - Incluir as bibliotecas necessarias para execução do código;
// 2 - {} Tudo que estiver dentro das chaves será executado, e apenas o que estuver dentro;
// 3 - \n = faz com que seja quebrada uma linha;
// 4 - Antes de executar o programa devemos compilar;
// 5 - Se for efetuada qualquer alteração no programa é necessário compilar novamente.
